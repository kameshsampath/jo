apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: hello-world
spec:
  template:
    spec:
      containers:
        - name: hello-world
          image: jbang://github.com/kameshsampath/jo/examples/http/server